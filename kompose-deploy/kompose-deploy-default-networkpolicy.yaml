apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: kompose-deploy-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/kompose-deploy-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/kompose-deploy-default: "true"
