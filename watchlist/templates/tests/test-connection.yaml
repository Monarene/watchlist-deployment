apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "watchlist.fullname" . }}-test-connection"
  labels:
    {{- include "watchlist.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "watchlist.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
